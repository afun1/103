# Server
PORT=3000
NODE_ENV=development

# Supabase
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# Vimeo API
VIMEO_CLIENT_ID=your_vimeo_client_id
VIMEO_CLIENT_SECRET=your_vimeo_client_secret
VIMEO_ACCESS_TOKEN=your_vimeo_access_token
VIMEO_FOLDER_ID=26555277

# Uploads (optional overrides)
MAX_FILE_SIZE=104857600
UPLOAD_DIR=./uploads

# Instructions:
# 1. Visit https://developer.vimeo.com/apps and create (or reuse) an app.
# 2. Generate a personal access token with scopes: public, private, upload, edit.
# 3. Paste the generated client ID/secret and access token above.
# 4. In Supabase, create an anon key (for browser) and service key (for server).
# 5. Duplicate this file to `.env.local` (or `.env`) and fill in real values.